<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.aiyakeji.mytest">

    <application
        android:name=".system.AppContext"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".ui.MainActivity"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


        <activity android:name=".ui.OneActivity" />
        <activity android:name=".ui.TwoActivity" />
        <activity android:name=".ui.ThreeActivity" />
        <activity
            android:name=".ui.FourActivity"
            android:screenOrientation="portrait" />
        <activity android:name=".ui.FiveActivity" />
        <activity android:name=".ui.SixActivity" />
        <activity android:name=".ui.SevenActivity" />
        <activity android:name=".ui.EightActivity" />
        <activity android:name=".ui.MVPLoginActivity" />
    </application>

</manifest>